# SPEC-004 adapter wiring

See Python module `cli_workflow.tools.adapters` for the machine-readable import
map and load helpers used by live drivers.

Adapters are **not** vendored here. They live in SpiderFeet:

`C:\projects\spiderfeet\.seed\scripts\cli_corpus\`

Set `SPIDERFEET_ROOT` if the sibling checkout path differs.
